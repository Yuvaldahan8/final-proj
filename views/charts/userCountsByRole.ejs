<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Counts By Role</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body>
    <div style="width: 75%;">
        <canvas id="userCountsByRoleChart"></canvas>
    </div>
    <script>
        const ctx = document.getElementById('userCountsByRoleChart').getContext('2d');
        const data = {
            labels: <%- JSON.stringify(userCounts.map(item => item._id)) %>,
            datasets: [{
                label: 'User Counts by Role',
                data: <%- JSON.stringify(userCounts.map(item => item.count)) %>,
                backgroundColor: 'rgba(153, 102, 255, 0.2)',
                borderColor: 'rgba(153, 102, 255, 1)',
                borderWidth: 1
            }]
        };
        const config = {
            type: 'bar',
            data: data,
            options: {
                scales: {
                    y: {
                        beginAtZero: true
                    }
                }
            }
        };
        new Chart(ctx, config);
    </script>
</body>
</html>
